<p id="notice"><%= notice %></p>

<p>
  <strong>Number:</strong>
  <%= @train.number %>
</p>

<p>
  <strong>On the station:</strong>
  <% if @train.current_station.present? %>
    <%= @train.current_station.title %>
  <% else %>
    Undefined 
  <% end %>  
</p>

<%= link_to 'Edit', edit_train_path %>
<%= link_to 'Back', trains_path %>
